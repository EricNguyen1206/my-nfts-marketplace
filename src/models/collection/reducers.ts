import { CollectionModel } from "./typings";

export const initialState: CollectionModel = {
    data: null,
    nftList: [],
    pending: false,
    error: false,
};

const reducers = {};

export default reducers;
